{
  "name": "@niflheim/core",
  "sideEffects": false,
  "type": "module",
  "scripts": {
    "build": "tsup",
    "fmt": "tsc && prettier --write 'src/**/*.{ts,tsx}' && eslint --fix ./src",
    "publish": "build && changesets publish"
  },
  "peerDependencies": {
    "typescript": "4.8.4",
    "rxjs": "7.5.7"
  }
}

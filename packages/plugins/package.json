{
  "name": "@niflheim/plugins",
  "sideEffects": false,
  "type": "module",
  "scripts": {
    "build": "tsup",
    "test": "vitest run --coverage",
    "fmt": "tsc && prettier --write 'src/**/*.{ts,tsx}' && ESLINT_USE_FLAT_CONFIG=false eslint --fix ./src"
  },
  "peerDependencies": {
    "rxjs": "7.5.7",
    "typescript": "4.8.4"
  }
}

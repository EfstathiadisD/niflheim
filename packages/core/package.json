{
  "name": "@niflheim/core",
  "sideEffects": false,
  "type": "module",
  "scripts": {
    "build": "tsup",
    "test": "vitest --coverage",
    "bench": "vitest bench",
    "fmt": "tsc && prettier --write 'src/**/*.{ts,tsx}' && eslint --fix ./src"
  },
  "peerDependencies": {
    "rxjs": "7.5.7",
    "typescript": "4.8.4"
  }
}
